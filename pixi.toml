[workspace]
name = "towerdefnapst"
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
nodejs = ">=20,<23"

[tasks]
dev = { cmd = "node scripts/dev-server.mjs", env = { HOST = "0.0.0.0", PORT = "4425" } }
check = "npm run check"
