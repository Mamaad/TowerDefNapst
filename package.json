{
  "name": "towerdefnapst",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Tower Defense fantasy élémentaire — rendu Canvas 2.5D procédural.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check scripts/dev-server.mjs && node scripts/check-modules.mjs && node tests/core-smoke.mjs && node tests/render-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
