{
  "name": "towerdefnapst",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tower Defense fantasy élémentaire jouable, rendu Canvas 2D procédural.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check scripts/dev-server.mjs && node scripts/check-modules.mjs && node tests/core-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
