import assert from 'node:assert/strict';import { TOWERS,levelStats } from '../src/config/towers.js';import { ELEMENTS,elementalMultiplier } from '../src/config/elements.js';import { ENEMIES } from '../src/config/enemies.js';import { getWave } from '../src/config/waves.js';import { PATH,BUILD_PADS } from '../src/config/map.js';
assert.equal(TOWERS.length,12);assert.equal(Object.keys(ELEMENTS).length,6);assert.ok(BUILD_PADS.length>=16);for(const t of TOWERS){assert.ok(levelStats(t,3).damage>levelStats(t,1).damage);assert.ok(t.description);}
assert.ok(elementalMultiplier('ice',{resistanceProfile:'ember'})>1.4);assert.ok(elementalMultiplier('fire',{resistanceProfile:'ember'})<.5);assert.ok(getWave(30).groups.some(g=>g.type==='boss'));assert.ok(ENEMIES.boss.boss);for(let i=1;i<PATH.length;i++)assert.ok(Math.hypot(PATH[i].x-PATH[i-1].x,PATH[i].y-PATH[i-1].y)>80);console.log('Core smoke tests passed');
