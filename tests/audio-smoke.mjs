import assert from 'node:assert/strict';import { readFileSync } from 'node:fs';
const audio=readFileSync(new URL('../src/systems/AudioManager.js',import.meta.url),'utf8'),battle=readFileSync(new URL('../src/systems/BattlefieldAudioManager.js',import.meta.url),'utf8'),wave=readFileSync(new URL('../src/systems/WaveManager.js',import.meta.url),'utf8'),combat=readFileSync(new URL('../src/systems/CombatSystem.js',import.meta.url),'utf8');
for(const feature of['kick(','snare(','hat(','pluck(','horn(','scheduleStep(',"'calm'","'wave'","'boss'"])assert.ok(audio.includes(feature),`organic score layer missing ${feature}`);
assert.match(audio,/startAmbience/);assert.match(audio,/DynamicsCompressor/);assert.match(audio,/playShot\(tower\)/);assert.match(audio,/playImpact\(projectile,enemy\)/);assert.match(audio,/lastSfx/);assert.match(battle,/musicState === 'intense'/);assert.match(battle,/setCombatPressure/);assert.match(wave,/setMusicState\(bossWave \? 'boss' : 'wave'\)/);assert.match(combat,/audio\.playShot\(tower\)/);console.log('Audio direction structure tests passed');
