import { PATH } from '../config/map.js';
export class Enemy{
 constructor(def,scale=1){this.def=def;this.id=crypto.randomUUID?.()||Math.random().toString(36);this.maxHp=def.hp*scale;this.hp=this.maxHp;this.speed=def.speed*(1+Math.min(.28,(scale-1)*.08));this.reward=Math.round(def.reward*(.9+scale*.1));this.x=PATH[0].x;this.y=PATH[0].y;this.pathIndex=0;this.progress=0;this.dead=false;this.escaped=false;this.shield=(def.shield||0)*Math.sqrt(scale);this.maxShield=this.shield;this.effects=new Map();this.distance=0;this.resistanceProfile=def.resistanceProfile;this.angle=0;this.hitFlash=0;this.hitPulse=0;this.phase=Math.random()*Math.PI*2;this._damageTextCooldown=0;}
 addEffect(name,data){const old=this.effects.get(name);if(old&&name==='poison'){old.stacks=Math.min(6,(old.stacks||1)+(data.stacks||1));old.remaining=Math.max(old.remaining,data.remaining);old.dps=Math.max(old.dps,data.dps);return;}this.effects.set(name,{...data});}
 update(dt){this.hitFlash=Math.max(0,this.hitFlash-dt*8);this.hitPulse=Math.max(0,this.hitPulse-dt*6);this._damageTextCooldown=Math.max(0,this._damageTextCooldown-dt);let speedMul=1,stunned=false,burn=0,poison=0,source=null;for(const [name,e] of this.effects){e.remaining-=dt;if(name==='slow')speedMul*=1-e.amount;if(name==='freeze'||name==='stun')stunned=true;if(name==='burn'){burn+=(e.dps||0)*(e.stacks||1);source=e.source||source;}if(name==='poison'){poison+=(e.dps||0)*(e.stacks||1);source=e.source||source;}if(e.remaining<=0)this.effects.delete(name);}if(burn>0&&!this.dead)this.takeRawDamage(burn*dt,false,false);if(poison>0&&!this.dead)this.takeRawDamage(poison*dt,true,false);if(this.dead&&source)this.killedBy=source;if(this.def.regen&&!this.dead)this.hp=Math.min(this.maxHp,this.hp+this.def.regen*dt);if(stunned||this.dead)return;let move=this.speed*speedMul*dt;while(move>0&&this.pathIndex<PATH.length-1){const b=PATH[this.pathIndex+1],dx=b.x-this.x,dy=b.y-this.y,dist=Math.hypot(dx,dy);this.angle=Math.atan2(dy,dx);if(dist<=move){this.x=b.x;this.y=b.y;move-=dist;this.distance+=dist;this.pathIndex++;}else{this.x+=dx/dist*move;this.y+=dy/dist*move;this.distance+=move;move=0;}}if(this.pathIndex>=PATH.length-1){this.escaped=true;this.dead=true;}this.progress=this.pathIndex+this.distance/10000;}
 takeRawDamage(amount,ignoreShield=false,feedback=true){if(this.dead)return 0;if(feedback){this.hitFlash=1;this.hitPulse=1;}let dealt=0;if(!ignoreShield&&this.shield>0){const absorb=Math.min(this.shield,amount);this.shield-=absorb;amount-=absorb;dealt+=absorb;}if(amount>0){const reduced=amount*(1-(this.def.armor||0));this.hp-=reduced;dealt+=reduced;}if(this.hp<=0){this.hp=0;this.dead=true;}return dealt;}
}
