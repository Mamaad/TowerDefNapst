import { COMBINED_ATTACKS } from '../config/worldStrategy.js';
export class TowerWorldManager{constructor(game,rng){this.game=game;this.rng=rng;this.comboCooldowns=new Map();this.clock=0}update(dt){this.clock+=dt;for(const[key,value]of[...this.comboCooldowns]){const next=value-dt;if(next<=0)this.comboCooldowns.delete(key);else this.comboCooldowns.set(key,next)}for(const tower of this.game.towers){tower.sabotageTimer=Math.max(0,(tower.sabotageTimer||0)-dt);this.game.world?.progression?.refreshTower?.(tower)}if(this.clock>=.55){this.clock=0;this.tryCombinedAttack()}}tryCombinedAttack(){const towers=this.game.towers.filter(t=>t.level>=3&&!t.sabotageTimer);if(towers.length<2)return false;for(const attack of COMBINED_ATTACKS){if(this.comboCooldowns.has(attack.id))continue;const a=towers.find(t=>t.def.element===attack.elements[0]);if(!a)continue;const b=towers.find(t=>t!==a&&t.def.element===attack.elements[1]&&Math.hypot(t.x-a.x,t.y-a.y)<=235);if(!b)continue;const cx=(a.x+b.x)/2,cy=(a.y+b.y)/2,targets=this.game.enemies.filter(e=>!e.dead&&Math.hypot(e.x-cx,e.y-cy)<=205);if(targets.length<4)continue;const base=(a.stats.damage+b.stats.damage)*attack.damage*.5;for(const enemy of targets.slice(0,12)){const dealt=enemy.takeRawDamage(base,false,true,attack.id==='volcanic-strike');a.registerDamage(dealt*.5);b.registerDamage(dealt*.5);if(attack.control)enemy.addEffect('stun',{remaining:attack.control,bypassWard:true});if(enemy.dead)enemy.killedBy=a}this.game.world.surfaces.create(attack.surface,cx,cy,{radius:102,duration:11,power:1.45,source:a,persistent:true});this.game.particles?.damageText?.(cx,cy-30,attack.name,attack.color,true,12);this.game.particles?.burstEvent?.(attack.id==='volcanic-strike'?'explosion':'shock',cx,cy,attack.color,1.15,.55);this.game.particles?.beam?.([{x:a.x,y:a.y},{x:cx,y:cy},{x:b.x,y:b.y}],attack.color);this.game.renderer?.kickCamera?.(.035,cx,cy);this.game.triggerHitStop?.(.025);this.game.audio?.playUi?.('combo');this.comboCooldowns.set(attack.id,attack.cooldown);return true}return false}reset(){this.comboCooldowns.clear();this.clock=0}}
